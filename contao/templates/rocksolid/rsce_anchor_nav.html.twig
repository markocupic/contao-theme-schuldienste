<div class="d-none d-md-block {{ class }} block"{{ cssID }}>
    <div class="">
        <ul class="d-flex">
            {% for anchor in anchorPoints %}
                <li>
                    <a class="js-scroll-trigger" href="{{ insert_tag('env::request') }}#{{ anchor.cssId }}">{{ anchor.title }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
